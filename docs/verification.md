# Verification status

Last updated 2026-09-10.
This is an engineering checkpoint, not a claim that live provider or native acceptance has passed.

## Electron 0.2.0 implementation candidate

Implemented on 2026-09-10: recording and pause/resume, microphone/system-audio adapters, Valsea streaming/translation/summaries, protected keys, final clipboard dictation, 200-point companion, Mac calendar preparation, audio import, and portable read-only MCP. The product is named Chirpberry and uses its own `Chirpberry Desktop` profile. The native app is preserved.

Pre-review local evidence (before these fixes):

- TypeScript checks and production bundling pass. Seventeen unit/integration checks pass: storage/recovery, Swift interchange, continuous PCM, WebSocket authentication/readiness/draining, failure/cancellation, final-only clipboard delivery, summary parsing/error privacy, and read-only MCP.
- Development and packaged Mac notebook acceptance pass: edit/restart, import/export with stubbed OS dialog selections, trash, search, keyboard controls, sandbox isolation, and automated accessibility in wide/light and compact/dark layouts.
- Separate synthetic Electron recording acceptance passes: disclosure, transient drafts, pause/resume, last-final save and clipboard, summary, audio import, settings accessibility, companion width 200/52, and rejection of notebook-store access from the companion. This build uses labelled fixtures, has no live microphone/provider access, and is excluded from packaging.
- The actual Mac helper compiles from shared native sources and responds to its private protocol. The packaged app's deep strict code-signature verification passes; it is ad-hoc signed and unnotarized. Production archive inspection found no fixture/test modules.

Release remains gated on real microphone/system-audio permission and capture, a saved Valsea key and successful live transcript/summary/import, physical Fn outside the app, calendar authorization, Windows/Linux runtime and clean-install evidence, and clean-source publication checks. No claim of perfect behavior or live transcription follows from fixture tests. CI now covers Mac, Windows, and Linux notebook/fixture tests and installer builds; results must be recorded after those jobs run.

## Pre-review candidate observations (R14)

The Codex driver collected these observations during the implementation session before the review fixes. They were not performed or supplied by Myko. They were not repeated in the focused fix rounds and do not establish acceptance of the changed source or final clean-source artifacts. The raw measurement remains outside Git as `desktop/test-results/idle-measurement.json` in the original workspace.

- The candidate DMG was mounted read-only; strict deep signature verification passed, its `app.asar` hash matched the unpacked app, and `INSTALL.txt` was present. This is candidate packaging evidence, separate from exact clean-commit DMG/source archive/checksum provenance.
- A packaged empty notebook with the bar collapsed and the real Mac helper idle on Darwin 25.5.0 arm64 launched in 2661 ms. After a 3-second warmup, six samples spaced 5 seconds apart each recorded 6 processes and 604 MiB aggregate RSS. CPU samples were 3%, 0%, 0%, 0.1%, 0%, and 0%.
- Aggregate RSS double-counts shared pages. This was one warm-cache local idle sample, not an energy measurement or recording-performance result. No key was saved and no audio/provider action occurred.
- The normally launched candidate's actual native accessibility tree exposed credential, disclosure, and shortcut Settings controls. This does not verify permission grants or physical Fn behavior.
- Both Electron E2E tests and `scripts/verify.sh` passed locally before these review fixes. Their historical results do not verify the changed source. The full pipeline and live acceptance gates remain separate from focused review regression tests.

## Review regression scope

R1–R12 were confirmed against the implementation. Capture controls now follow the recording owner independently of the selected note, and an existing active companion cannot be hidden until capture is idle. Native quit propagates save failure and shares in-progress stop/abort work; provider finalization failures suppress dictation delivery, and unsolicited terminal events stop capture. Summary responses compare the current summary and actions against the request snapshot. Both stores check the exact serialized bytes before replacement; Electron applies the same check in the editor. Mac installation uses a distinct candidate folder, and release preparation uses Node's npm entrypoint and fresh artifact staging.

Regression coverage lives in `desktop/tests/e2e/review.spec.ts`, `desktop/tests/store.test.ts`, `desktop/tests/release.test.mjs`, `macOS/Tests/ChirpberryAppTests/ReviewRegressionTests.swift`, and `macOS/Tests/ChirpberryCoreTests/MeetingStoreSizeTests.swift`. Transport, capture, and release-command fixtures verify failure handling and provenance selection logic; they are not live Valsea, hardware, Windows runtime, or installer acceptance evidence. R13 lacked a description in the first fix round; the subsequent lifecycle review below uses its full supplied description.

Final focused verification on 2026-09-10: desktop TypeScript checking passed; 13 selected storage/release tests passed; the fixture build and two new Electron UI tests passed; two native serialized-size tests passed; six existing native companion model tests passed; and all six new native review regression tests passed. The initial native regression run exposed two test-harness errors (native dictation spacing and an overfulfilled expectation); after correcting those tests, the regression class passed. `git diff --check` passed.

Commands: `npm run typecheck` and `node --import tsx --test tests/store.test.ts tests/release.test.mjs` from `desktop/`; `node scripts/build.mjs --fixture` and `playwright test tests/e2e/review.spec.ts --workers=1` from `desktop/`; `swift test --package-path macOS --filter MeetingStoreSizeTests`; and `xcodebuild test` using the `ChirpberryModelTests` scheme with `-only-testing:ChirpberryModelTests/ReviewRegressionTests` and `-only-testing:ChirpberryModelTests/CompanionModelTests`. The corrected native rerun selected only `ReviewRegressionTests`.

This fix round did not run the complete repository test/lint suite or `scripts/verify.sh`; those remain the dedicated pipeline gates. It did not build final clean-source installers, publish artifacts, contact Valsea, or claim new live OS/provider acceptance.

## Subsequent lifecycle review (R13, R15, R16)

The complete R13 and the R15/R16 findings were confirmed. Native helper shutdown now retains an idempotent termination promise until child exit; the 1500 ms force-kill timer remains referenced. Capture cancellation retains that teardown for later Stop/shutdown callers, and stopping a dead or destroyed capture helper cannot spawn a replacement. Runtime shutdown awaits its native bridge before Electron exits. Closing the companion reaches the same capture cancellation path as notebook closure and defers ordinary window closure until capture stops. A full Stop upgrades a draining Pause, waits for finalization and persistence, and delivers dictation at most once; cancellation or closure suppresses delivery.

Regression coverage uses `desktop/tests/native.test.ts` with real child processes running `desktop/tests/fixtures/native-helper.mjs`, controlled provider/disk barriers in `desktop/tests/recording.test.ts`, and `desktop/tests/e2e/lifecycle.spec.ts`. The child helper intentionally hangs on selected requests or stdin closure and uses no native audio APIs or credentials. Electron tests stub clipboard writes within the test process and exercise both the bar action and the helper shortcut event path. This does not establish physical Fn, live provider, permission-dialog, or Windows device acceptance.

Final focused lifecycle verification on 2026-09-10 passed: desktop TypeScript checking, production and fixture bundling, 18 selected native-child/recording tests, and seven affected Electron UI scenarios. The UI scenarios include companion closure during a pending native start, closure of either surface during Pause draining, bar Stop and helper shortcut completion during Pause, and the prior collection-navigation/active-bar regressions. Tests assert child process exit when teardown or application shutdown completes, and preserve original notes and final-only delivery. Clipboard logging is confined to the synthetic fixture and its worktree test directory.

Commands from `desktop/`: `npm run typecheck`; `node --import tsx --test tests/native.test.ts tests/recording.test.ts`; `node scripts/build.mjs`; `node scripts/build.mjs --fixture`; and `playwright test tests/e2e/lifecycle.spec.ts tests/e2e/review.spec.ts --workers=1 --max-failures=1`. The UI harness was corrected for async clipboard types, Playwright's dynamic-import restriction, and editor focus during fixture setup before the passing UI rerun. `git diff --check` passed. The complete repository test/lint pipeline and live device/provider gates were not run in this focused fix round.

## Clipboard completion and pipeline checkpoint

Electron 44 clipboard writes return promises. Stop now awaits the write before reporting success or completing shutdown, and catches asynchronous rejection while preserving the saved dictation. Two controller regression tests failed before the fix and passed afterward. Additional synthetic Electron tests cover delayed success, rejected writes, and quit while delivery is pending. They isolate the OS clipboard and verify final-only persistence and at-most-once delivery.

The pipeline's latest test step passed 40 desktop tests, Swift core/MCP checks, the native Release build and model tests, Electron UI checks, the packaged Mac notebook, DMG integrity/signature verification, and isolated website browser checks. A fresh-checkout icon-directory failure was corrected. Earlier test issues were also corrected: the compact test now selects the notebook window, Trash navigation awaits the completed state change, and website tests use an isolated server. A packaged notebook retry subsequently passed five consecutive runs. These are local and synthetic results, not live platform/provider acceptance.

The no-mistakes run `01M24X7E33PA0HGAAXA48B97ZG` completed its review and test stages, then failed during documentation because its Codex CLI backend exhausted its usage quota. Automated document/lint/push/PR/CI stages did not complete. Commits `c06c536`, `bd81dcf`, and `a267198` preserve the review and clipboard fixes. The release checkout retains them; the original dirty checkout was not reset. The final uncommitted clipboard tests and icon-directory fix were recovered after the tool removed its failed shadow worktree. Do not label the overall automated pipeline passed.

Direct verification after recovery on 2026-09-10 passed: `scripts/verify.sh` (28 Swift core tests, MCP process checks, three site contract tests and its production build, native capture-helper build, 40 desktop tests with TypeScript/production build, native Release build, and 24 native model/shortcut/hover tests); `npm run test:e2e --prefix desktop` (all 12 scenarios); and `git diff --check`. The synthetic clipboard stubs now match Electron 44's asynchronous types. The captured 780-point dark notebook, clipboard failure with saved notes, and 200-point bar were visually inspected. The transcript count's singular label was corrected afterward and type checked. Both desktop and website dependency audits returned zero known vulnerabilities; that is not proof of absence of security defects.

## Earlier native local evidence

- 26 Swift core tests: transcript source/translation semantics, provisional/final state, duplicate handling, speaker scope, JSON round-trips, corrupt-file recovery, file permissions, exports, summaries, search, HTTP error handling, upload size/header boundaries, Unicode limits, continuous PCM conversion at 44.1/48 kHz, scratchpad compatibility, selected-text formatting, and inward expansion geometry at all four screen edges.
- Eighteen native model/shortcut/clipboard/hover tests: scratchpad persistence/relaunch and independent selection, disclosure without capture, simultaneous-session exclusion, reversible Trash, suppression of delivery on window closure including finalization, storage failure before connection, held-key repeat suppression, Fn activation once on release, combination rejection, cancellation of queued shortcuts, isolated clipboard final delivery/preservation on empty, failed, and cancelled sessions, and hover opening/closing with re-entry, feedback, keyboard-focus, and popover interactions.
- MCP process tests: initialization, discovery, read-only annotations, search, full reads, invalid UUID rejection, and trash exclusion.
- Three website release-contract tests and the production Vite build.
- Four Brave/Playwright browser tests: WCAG A/AA automated audit, widths from 320 to 1440 pixels, language examples, installation help, both copy buttons, clipboard denial fallback, and JavaScript-disabled navigation.
- Desktop and phone website screenshot review.
- Native Release arm64 compilation and ad-hoc signature verification on macOS 26.5.1 with Xcode 26.
- Desktop companion native UI checks on an unlocked Mac using isolated synthetic notes: rendered bar and Scratchpad, disclosure/cancel without capture, upcoming-meetings connection state, keyboard focus, in-app shortcut equivalents, editing, selected-text formatting and Undo, Copy feedback, no-match search, sidebar collapse, and saved notes after quit/relaunch.
- Fixed docking UI checks in the built app: Settings exposes the four edge choices; Top, Left, and Bottom update the rendered orientation; Escape returns horizontal and vertical controls to the small handle; and Bottom survives quit/relaunch. The final build's menu no longer exposes a centering command. Dragging is disabled at the NSPanel level and no drag gesture is exposed. The app was left collapsed at Bottom.
- Compact bar screenshot review: the idle horizontal window is 240 points wide (previously 300), with a 60-point resting capsule (previously 78). In an isolated notebook with the first-use disclosure forced on through launch arguments, Tab opened dictation setup without recording; Cancel left capture idle. Switching Dictation key to Control–Option–D restored normal Tab navigation. The OS accepted the global registrations; the automated key press exercises the app-local delivery path, not a physical key in another app.
- Appearance update: reviewed the rebuilt idle bar without its separate name/hover badge. The capsule uses a lighter lavender-gray background at 62% opacity and a softer shadow; the horizontal window is now 60 points tall. Original actions remain present with their accessibility labels.
- Further width reduction and Fn clipboard mode: reviewed the built 220-point horizontal window (from 240), with a 52-point resting handle (from 60). The actual app shows Dictate · Fn and Settings selects Fn / Globe · clipboard. The executable launched from the development environment reported no event-tap registration error; this does not establish permission for a normal app launch. Control–Option–D opened the disclosure naming Clipboard as the destination. Cancel left capture idle. Starting with no saved Valsea key displayed the expected Settings instruction and returned to idle without audio or provider access. These checks used an isolated document directory; the first-use disclosure was left enabled afterward. UI automation cannot synthesize Fn, so physical Fn and Fn combinations remain unverified.
- Requested 200-point width: reduced the idle horizontal window from 220 to 200 points, with tighter spacing and a 40-point microphone button. Reviewed the rebuilt bar with all five buttons visible and unclipped. The 52-point resting handle remains unchanged. `scripts/verify.sh` passed, and the updated app was reopened.
- Hover responsiveness: five regression tests failed against the prior controller and passed after reducing exit delay from 450 to 120 ms, releasing keyboard hold after pointer interaction, and allowing idle collapse while feedback remains available. Re-entry cancels the pending close; popovers retain the bar. The content transition is now 80 ms and the AppKit tracking area is retained across layout changes. `scripts/verify.sh` passed. In the rebuilt app, keyboard focus exposed all five controls and Escape restored the resting handle; the 200-point bar was visually reviewed and left collapsed. These checks do not measure physical pointer-event timing.

The PCM tests found and fixed a converter callback that supplied more frames than requested.
The website audit found and fixed insufficient contrast in small preview labels.
Fixture responses are synthetic test data and are not evidence of real Valsea success.

## Still required

- Native light/dark appearance, narrow-window behavior, keyboard and VoiceOver labels on an unlocked Mac.
- Real microphone permission denial/approval, start, pause, resume, stop, closing the window, and quitting during capture.
- System-audio permission and capture from a consented test call, including headphones and both input streams.
- Live Valsea session readiness, final source/translation pairing, last-segment delivery on Stop, a structured summary, and audio import.
- Real connection failure, insufficient-credit handling where available, and API-key save/reload.
- Native create/edit/relaunch persistence, export/import, speaker rename, trash/restore, calendar authorization, and Apple Intelligence availability paths.
- Live dictation and final clipboard output, external-editor insertion in alternate shortcut modes, Accessibility permission denial/approval, physical Fn activation and Fn combinations outside Chirpberry, dark appearance, narrow-window and full-screen Spaces acceptance. The companion UI checks above do not establish these results.
- Physical pointer hover/exit timing while another app is active, screen disconnection, and multi-display docking. The UI automation used here has no pointer-only hover operation.
- No-mistakes pipeline review and GitHub CI.
- Exact-commit DMG packaging and mount verification, public asset download checksum, Homebrew installation, and production site download.

The companion UI session ran on an unlocked Mac. Earlier full-notebook acceptance remains incomplete.
Provider acceptance is waiting for a Valsea credential; the companion session confirmed there is no saved Chirpberry Valsea Keychain item without reading or exposing credentials.
The final app launched normally reports Fn unavailable. System Settings has an enabled Chirpberry Accessibility entry, but the current build still needs its authorization resolved. The standard Add flow for authorizing the current build is waiting at the macOS Touch ID/password prompt; no credentials were requested in chat and no OS permission database was modified. Returning to Chirpberry after authorization retries Fn registration.
No credential or paid credit purchase is included in the repository.
These gates must be completed and recorded before marking the first release verified.

## Release-candidate follow-up review (R1–R4, R18)

The five findings were confirmed against `055f7fb`. Companion renderer termination now uses the shared cancellation owner, suppressing clipboard delivery and awaiting capture-helper exit. Native imports preserve concurrent finals and notes across transcription and translation, locating the imported segment by UUID. Numeric validation runs before native decode or persistence, and display conversions tolerate invalid in-memory values. Scratchpad consumes formatting identity above the recreated editor and synchronizes native undo/redo with its notes binding. Source archives run from the repository root; the incomplete `held-055f7fb-incomplete-source` artifact set remains on HOLD and must never be published.

Focused verification on 2026-09-10 passed: six release tests including real Git archive creation from a nested desktop working directory; desktop TypeScript checking and fixture bundling; three actual Electron renderer-crash scenarios during connecting, recording, and draining Pause; four native numeric tests; the controlled import regression covering live finals, note edits, segment reordering, and translation success/failure; invalid-JSON import preservation; and the hosted native Notes/Summary recreation test with Undo/Redo. The native Debug app build also passed. Initial editor runs exposed view-update mutation and missing undo-to-binding synchronization; focused editor reruns verified the corrections. The final whitespace check passed.

Commands: from `desktop/`, `node --test tests/release.test.mjs`, `tsc --noEmit`, `node scripts/build.mjs --fixture`, and `playwright test tests/e2e/lifecycle.spec.ts --grep 'companion renderer termination' --workers=1`; from the repository root, `swift test --package-path macOS --scratch-path .build/review-core --filter MeetingNumericTests`, and `xcodebuild test` with the `ChirpberryModelTests` scheme selecting only `ReviewRegressionTests/testAudioImportPreservesConcurrentLiveFinalsAndNotesAcrossBothAwaits`, `NumericImportTests`, and `NoteTextEditorTests`. Editor reruns selected only `NoteTextEditorTests`; native compilation used `xcodebuild build` with the `Chirpberry` scheme and Debug configuration. Test data and build output were isolated in this worktree.

Earlier passing suite evidence above is retained. This round did not run the complete repository test/lint suite, `scripts/verify.sh`, or release packaging. Capture/provider tests use synthetic adapters, and the archive regression uses synthetic installer bytes with real Git operations. No credentials, permission changes, merge, publication, or live OS/provider/device acceptance are implied by these results.

## Electron notebook simplification — 2026-09-10

The Electron notebook now opens with a compact notes sidebar and a focused editor. Transcript is optional, capture controls stay visible across collection changes, and secondary actions use keyboard-accessible overflow/context menus. New notes focus the editor, and the native Search command restores a hidden sidebar. The 200-point companion keeps its existing hover timing with a lighter appearance and actual clipboard-completion feedback. The installed Wispr Flow scratchpad was inspected for interaction reference; only shared design conclusions are recorded in `docs/references.md`.

Verification passed in the isolated `build/notebook-simplification` checkout: `scripts/verify.sh` (32 Swift core tests, MCP process checks, three site contract tests and production build, native capture-helper build, TypeScript checking, 41 desktop tests and production bundle, native Release build, and 27 native model tests); all 17 scenarios from `npm run test:e2e --prefix desktop`; and `git diff --check`. The new focused scenario covers note creation, collapsed navigation, native menu search, keyboard menu focus/Escape, row pinning, notebook movement, content preservation, viewport-clamped popovers, and an automated accessibility audit. A context-menu dismissal defect was reproduced and fixed; the focused scenario also passed after clearing consumed menu-open requests.

`npm run package --prefix desktop` produced an ad-hoc-signed Mac app. Both production notebook scenarios (`notebook.spec.ts` and `simplification.spec.ts`) passed against that packaged executable with isolated documents and OS integrations disabled. Light/dark, narrow-window, focused-writing, open-menu, live-draft, and companion screenshots were visually inspected. The package passed strict deep signature verification, and the installed candidate's `app.asar` matched the tested package (SHA-256 `d46e1b129540ba8167e4b8b88a67f3b84e94e5aa3d1041735e8b54c8872df1f1`). A normal launch of the updated installed app was inspected; Command-F restored the hidden sidebar and focused Search. The previous installed app was preserved privately for rollback, and existing documents were left in their original stores.

## Electron meeting assistant — 2026-09-10

The isolated `build/meeting-assistant` branch adds Upcoming calendar tracking, reviewed copy/Markdown sharing, and streaming OpenAI meeting questions, catch-up, suggested questions, and editable response drafts. See [the implementation contract and official demos](meeting-assistant.md).

`scripts/verify.sh` passed: 32 Swift core tests, MCP process checks, three website tests and production build, native capture-helper build, TypeScript checking, 51 desktop tests and production bundle, native Release build, and 27 native model tests. The new tests exercise split UTF-8/SSE responses through the production parser, incomplete/error/oversized streams, protected authorization headers and `store: false`, bounded context, immutable source excerpts, invalid citation IDs, cancellation/late updates, recurring calendar identities, refresh coalescing, disconnect races, denied/stale calendars, safe meeting links, and opt-in share sections.

All 19 Electron interaction scenarios passed. The new flow covers calendar → prepared note → final speech → streamed questions/source review → editable response → independent answer cancellation → recording stop → summary → reviewed copy and exact Markdown export, while preserving original notes. The calendar/audio/provider data is explicitly synthetic; a loopback HTTP server exercises the real OpenAI streaming client. Accessibility checks passed for live AI, sharing, Upcoming, and compact dark mode; their screenshots were visually inspected. A contrast defect was reproduced and fixed. Dark-mode screenshots use an explicit Playwright media override after an initial native-theme-only attempt did not update the test page.

Three production scenarios passed against the packaged Mac executable with isolated documents and OS integrations disabled: notebook persistence/import/export, simplified navigation/menus, and assistant setup gates plus exact share snapshots even after the note changes. The synthetic provider scenario correctly skips against the package. The package excludes fixture code, passed strict deep code-signature verification, and was installed as the separate Electron candidate after the idle app quit. Its installed `app.asar` matches the tested package at SHA-256 `070f0aac7f9bf204ec28086641d7ac5769be52ca68bc8f82da1ae0c66b2e10fe`. The previous installed app is retained privately for rollback; original native documents remain separate.

These results do not establish real microphone/system-audio capture, physical Fn behavior, live Valsea/OpenAI success, calendar account synchronization, Windows/Linux acceptance, or a public release. Direct Google/Microsoft OAuth and hosted sharing are not implemented. The website was built and checked, with no deployment in this change.

Normal installed-app Calendar acceptance exposed missing permission declarations: the main app lacked the purpose string and the signed app/helper lacked hardened-runtime Calendar access. Purpose strings and calendar/audio-input entitlements were added; both actual signed executables now pass `verify-mac-permissions.mjs`. That check rejects the retained prior app with its missing Calendar entitlement and is also required on future mounted release payloads. All six release regression tests passed after adding the gate, and the three production scenarios were rerun successfully against the final signed package. The installed app's main Info.plist and capture executable match that package. The corrected Calendar request remains pending manual macOS consent; computer control refuses access to the system consent dialog. No successful real calendar read is claimed.

These are local UI and synthetic capture results. The app remains an unnotarized candidate; live provider, physical Fn, microphone/system-audio permissions, Windows/Linux device acceptance, and public release gates remain separate. The root README's native instructions, architecture, and provider protocols still match their respective implementations and were left unchanged.
